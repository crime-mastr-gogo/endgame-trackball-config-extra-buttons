# The console device satisfies upstream board selections, but production emits
# neither USB logs nor printk output on the Studio RPC transport.
CONFIG_ZMK_USB_LOGGING=n
CONFIG_LOG_BACKEND_UART=n
CONFIG_SHELL_BACKEND_SERIAL=n
CONFIG_PRINTK=n

# Required internally by the BLE shell and ESB relay.
CONFIG_LOG=y
