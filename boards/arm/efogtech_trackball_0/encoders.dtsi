/*
 * Both rotary encoders have been removed.
 *
 * Their four signal pins are now configured as physical
 * switch inputs in buttons.dtsi:
 *
 * Right encoder: P1.08 and P1.09
 * Left encoder:  P0.17 and P0.16
 *
 * No encoder sensor nodes or sensor bindings remain.
 */